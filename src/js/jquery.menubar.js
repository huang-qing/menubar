(function ($) {
    jQuery.fn.menubar = function (options) {
        // menubar default settings:
        var defaults = {
            },

            settings = $.extend({}, defaults, options);

        return this.each(function () {
            // Plugin code would go here...
        });
    };
}(jQuery));

